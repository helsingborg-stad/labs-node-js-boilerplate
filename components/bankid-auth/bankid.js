const axios = require("axios");

exports.test = () => {
    return new Promise((resolve, reject) => {
        
        console.log("bankid")

        resolve ({name: "test"})

        if (err)
                return reject(err)
            else {
                resolve(reply);
            }
    })
}